# Test fixture audit (indexed)

Same audit shape as the negative fixture. The companion fixture backlog
file (`backlog-with-T-1.md` in this directory) DOES contain a row that
references both this filename and the finding ID `T-1`, so the gate
passes.

## CRITICAL

### T-1 -- Synthetic critical finding for positive regression test

**Severity:** CRITICAL.

This row is mirrored in the test backlog fixture.
